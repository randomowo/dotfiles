#!/bin/bash

# Import the colors
. "${HOME}/.cache/wal/colors.sh"


layout=$(ls ~/.screenlayout/ \
	| dmenu -nb "$color0" -nf "$color15" -sb "$color1" -sf "$color15" "$@")

[ ! -z $layout ] && sh ~/.screenlayout/"$layout"

exit 0
